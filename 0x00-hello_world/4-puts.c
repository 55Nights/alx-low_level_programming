#include<stdio.h>

/**
 * main- Entry point
 *
 * Return: will return 0 if successful
 */
int main(void)
{
	char str[100] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}

