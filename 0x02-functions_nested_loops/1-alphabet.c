#include "main.h"
/**
 * main - entry point
 * Return: if successfull return 1
 * @print_alphabet prints char a
 */
int main(void)
{
	 print_alphabet()
	{
		char a = 'a';

		for (a = 'a'; a <= 'a'; a++)
		{
			_putchar(a);
		}
	}
	_putchar('\n');

	return (0);

}
