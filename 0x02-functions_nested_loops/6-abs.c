#include "main.h"
#include <stdlib.h>
#include <stdio.h>
 /**
 * _abs - the entry point
 * @d : -  the value to be evaluated
 * Return: 0;
 */
int _abs(int d)
{
	int v;

	v = abs(d);
	printf("%d", v);

	return (0);
}
