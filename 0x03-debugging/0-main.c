#include "main.h"
#include <stdio.h>
/**
 * main - tests function that prints if integer is postive or negative
 * Return: 0
 */
int main(void)
{
	int i;

	i = 9;
	positive_or_negative(i);

	return (0);
}
