#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _atoi - converts string to integer
 * @s:  string to convert
 * Return: 0
 */
int _atoi(char *s)
{
	int r;

	r = atoi(s);
	return (r);
}
