#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcpy -  copys strings
 * @dest : first string
 * @src : last string
 * Return: char
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (src);
}
